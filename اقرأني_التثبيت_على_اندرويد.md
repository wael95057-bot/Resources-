# تشغيل التطبيق على أندرويد

هذا المجلد فيه تطبيق ويب كامل (PWA) شغّال بالعربي والإنجليزي، متجاوب مع الموبايل، وفيه:
- `index.html` — التطبيق نفسه
- `manifest.json` — بيانات التطبيق (الاسم، الأيقونة، الألوان) اللي تخلي أندرويد يعامله كتطبيق حقيقي
- `sw.js` — يخلي التطبيق يشتغل حتى لو النت وقع (تخزين مؤقت)
- `icon-192.png` / `icon-512.png` — أيقونة التطبيق (تقدر تستبدلها بلوجو خاص بيك)

## الطريقة الأسهل (بدون برمجة) — تثبيت كـ PWA على أندرويد

1. ارفع الملفات دي على أي استضافة تدعم HTTPS (GitHub Pages، Netlify، Vercel، Firebase Hosting... كلها مجانية).
2. افتح رابط الموقع من متصفح Chrome على موبايل أندرويد.
3. هيظهر تلقائيًا خيار **"إضافة إلى الشاشة الرئيسية" / "تثبيت التطبيق"** (Install App) في قائمة المتصفح (⋮).
4. بعد التثبيت، هتلاقي أيقونة التطبيق على الشاشة الرئيسية زي أي تطبيق عادي، وهيفتح بدون شريط المتصفح.

هذه الطريقة **لا تحتاج Android Studio ولا نشر على متجر Google Play** وتشتغل فورًا.

## لو عايز ملف APK حقيقي يترفع على Google Play

استخدم أداة **PWABuilder** (مجانية من مايكروسوفت):
1. روح على `https://www.pwabuilder.com`
2. حط رابط موقعك المرفوع (من الخطوة اللي فوق)
3. اختر Android package
4. هيولّدلك ملف `.aab` أو `.apk` جاهز يترفع على Google Play مباشرة

بديل آخر: أداة `bubblewrap` من جوجل (سطر أوامر) لو عايز تتحكم أكتر في الإعدادات.

## ملاحظة عن الأيقونة

الأيقونة الحالية بسيطة (placeholder). لو عندك لوجو جاهز، استبدل `icon-192.png` و`icon-512.png` بنفس المقاسات ونفس الاسم.
